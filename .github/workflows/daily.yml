name: Daily Spotify Playlist Refresh

on:
  workflow_dispatch:
  schedule:
    - cron: "5 8 * * *" # daily 02:05 CST / 03:05 CDT

jobs:
  refresh:
    if: ${{ false }}
    runs-on: ubuntu-latest

    steps:
      - name: Disabled
        run: echo "Daily Spotify playlist refresh is disabled in this branch."
