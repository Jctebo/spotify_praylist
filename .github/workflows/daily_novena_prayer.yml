name: Prayer Contracts Matrix (Disabled)

on:
  workflow_dispatch:

defaults:
  run:
    shell: bash

jobs:
  legacy-page-audio-disabled:
    runs-on: ubuntu-latest

    steps:
      - name: Legacy page-audio contract matrix disabled
        run: |
          echo "The legacy top-level config/*_page_audio.json contract matrix has been discontinued."
          echo "Those contract files now live under config/legacy/page_audio/."
          echo "This workflow is intentionally disabled."
