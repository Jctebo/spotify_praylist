# Release 0.3.12.3 — Novena Fallback Prose

Branch: fix/novena-fallback-no-truncation

## Scope

- Remove deterministic novena fallback metadata truncation and its length constraint.
- Preserve the generated-intro validation policy for model output.

## Research

- The July 30 Saint Peter Julian Eymard fallback had truncated biography and patronage fragments embedded in spoken sentences.

## Plan

- Remove fallback context compaction.
- Regression-test the complete Saint Peter Julian Eymard fallback prose.

## Progress

- Implemented and verified; pending release.

## QA

- `python -m unittest tests.test_publish_devotional_intro tests.test_novena_artifacts -q`
- `git diff --check`

## Release Notes

- Deterministic novena fallbacks now retain complete saint metadata instead of speaking truncated fragments.
