# Release 0.0.6.0 - add-next-prayer-back

- Status: Future
- Depends On: 0.0.5.6

## Purpose
- Add the next prayer back on top of the generic runner after Morning Prayer is stable.

## Scope
- New prayer config
- Any small adapter needed for that prayer
- Regression coverage for the new config path

## Not In Scope
- Rebuilding every prayer at once
- Changing the generic runner shape again

## Success Criteria
- The generic runner can execute a second prayer config without reintroducing old multi-prayer branches.
