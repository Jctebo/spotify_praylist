# Release 0.3.6.14: Novena Sidecar Daily Theme Freshness

Status: Shipped
Date: 2026-06-16

## Scope

Ensure target-date novena sidecars are refreshed when canonical daily theme prose changes, even when the existing sidecar is internally consistent and already has top-level daily theme metadata.

## Plan

- Compare target-date sidecars against the current cached daily theme context before reuse.
- Include canonical prose fields in sidecar writer freshness checks.
- Add regression coverage for stale canonical transition text.

## Progress

- Updated novena pipeline sidecar reuse to validate against the current daily theme context for publish target dates.
- Updated artifact writer freshness keys to include shared explanation, transition, and reflection focus.
- Added regression coverage for sidecars containing stale transition text.

## QA

- `python -m unittest tests.test_novena_pipeline tests.test_novena_artifacts`

## Release Notes

### Fixed
- Fixed novena sidecar reuse so target-date sidecars are compared against the current canonical daily theme prose, not only internally consistent metadata.
- Added regression coverage for refreshing stale canonical transition text in existing novena sidecars.
